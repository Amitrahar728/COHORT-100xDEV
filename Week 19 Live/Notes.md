

Having code in same files in NEXT.js led us to have same folder with frontend and backend.
which interestingly provide us a way to replace cors and defining their domain from where we can get requests 



{/* create a new folder with route name 
      inside which create a page.tsx for what you want to show on this page
      this is directly shown on page
      that is how routing occurs in NextJs



      We can also write the inside pages in react too.
       */}

       

right way to fetch data using nextjs :

if we are using useState then its like its request our localhost a lot before hitting backend actually 
means the request is sent by client by use Client in  page.tsx 



What we can use :
